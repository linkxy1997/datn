import { AuthenticationToken } from './authentication-token';

describe('AuthenticationToken', () => {
  it('should create an instance', () => {
    expect(new AuthenticationToken()).toBeTruthy();
  });
});
